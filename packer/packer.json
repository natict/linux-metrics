{
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": "",
    "subnet_id": "",
    "vpc_id": ""
  },
  "builders": [
    {
      "name": "linux-metrics-us-east-1",
      "type": "amazon-ebs",
      "access_key": "{{user `aws_access_key`}}",
      "secret_key": "{{user `aws_secret_key`}}",
      "region": "us-east-1",
      "source_ami": "ami-df38e6b4",
      "instance_type": "t2.small",
      "ssh_username": "ubuntu",
      "ami_name": "linux-metrics-us-east-1",
      "subnet_id": "{{user `subnet_id`}}",
      "vpc_id": "{{user `vpc_id`}}"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "script": "bootstrap.sh"
    }
  ]
}
